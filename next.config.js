/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'pbxt.replicate.delivery',
      '87wottj3rciug1va.public.blob.vercel-storage.com',
    ],
  },
};

module.exports = nextConfig;
